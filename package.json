{
  "name": "se-sitrep",
  "version": "1.0.0",
  "description": "Lightweight daily standup and status dashboard for small Agile teams",
  "type": "module",
  "scripts": {
    "lint": "eslint js/**/*.js",
    "test:unit": "vitest run",
    "test:unit:watch": "vitest",
    "test:e2e": "playwright test",
    "test": "vitest run && playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.0",
    "eslint": "^9.3.0",
    "jsdom": "^24.1.3",
    "serve": "^14.2.0",
    "vitest": "^2.0.0"
  }
}
